using UnityEngine;

public class Multireguñar : MonoBehaviour
{


    public string[][] Multiregular = new string[1][];
  
    void Start()
    {

        Multiregular[0] = new string[3] { "hola", "haz venido sola?", "o estas de pasadita?" };
        Multiregular[0] = new string[1] { "que rara tu ropa",};
        Multiregular[0] = new string[5] { "no creo que seas de aca", "que bellos", "que bellos son tus #####", "los miro y me enveneno", "pero no hablo tu idioma solo dices" };
        Multiregular[0] = new string[3] { "popopo", "popopo", "popopo" };

        MostrarTablaDentada(Multiregular);
    

}
    public void MostrarTablaDentada(string[][] jaggedArray)
    {
        for (int fila = 0;  fila < jaggedArray.Length; fila++)
        {
         for (int col = 0; col < jaggedArray[fila].Length; col++)
            {
                string value = jaggedArray[fila][col];
                Debug.Log(value);
            }                
        }
    }
    void Update()
    {
        
    }
}
